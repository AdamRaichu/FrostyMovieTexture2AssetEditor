<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:MovieTexture2AssetEditorPlugin"
    xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
    xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
    xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:MovieTexture2Editor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MovieTexture2Editor}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Video Player -->
                            <Border Grid.Row="0" Background="{DynamicResource ControlBackground}">
                                <vlc:VideoView x:Name="PART_VideoView" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <!-- Controls -->
                            <Grid Grid.Row="1" Background="#FF2D2D30">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>

                                <Button x:Name="PART_PlaybackButton" Grid.Column="0" Content="Play" Width="50" Margin="5" Padding="2"/>
                                
                                <Slider x:Name="PART_SeekSlider" Grid.Column="1" VerticalAlignment="Center" Margin="10,0" IsMoveToPointEnabled="True"/>
                                
                                <TextBlock Grid.Column="2" Text="Vol" Foreground="White" VerticalAlignment="Center" Margin="5,0"/>
                                <Slider x:Name="PART_VolumeSlider" Grid.Column="3" VerticalAlignment="Center" Margin="5" Minimum="0" Maximum="100" Value="100" />
                            </Grid>
                        </Grid>

                        <!-- Row 1: Splitter -->
                        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#FF3E3E42" ShowsPreview="True" ResizeBehavior="PreviousAndNext"/>

                        <!-- Row 2: Property Grid -->
                        <Border Grid.Row="2" Background="{DynamicResource ControlBackground}">
                            <ctrl:FrostyDockablePanel x:Name="PART_AssetPropertyGridPanel" Title="Properties" Icon="/FrostyEditor;component/Images/Properties.png">
                                <core:FrostyPropertyGrid x:Name="PART_AssetPropertyGrid" Asset="{Binding Asset, RelativeSource={RelativeSource TemplatedParent}}" Modified="{Binding AssetModified, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </ctrl:FrostyDockablePanel>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
